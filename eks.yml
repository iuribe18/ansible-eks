- hosts: localhost
  gather_facts: no
  tasks:
      - include_role:
           name: networking

- hosts: localhost
  gather_facts: no
  tasks:
      - include_role:
           name: eks

# - hosts: localhost
#   gather_facts: no
#   tasks:
#       - include_role:
#            name: argo